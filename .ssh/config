Match exec "~/.ssh/load_key.sh"
     ControlMaster auto
     ControlPath /tmp/ssh-%r@%h:%p
     ControlPersist 60

     ServerAliveInterval 5

     IdentitiesOnly yes

Include .colima/ssh_config
Include config.local
